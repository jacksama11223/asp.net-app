@model IEnumerable<SmartLMS.Models.CommunityResource>
@{
    Layout = "_CommunityLayout";
    ViewData["Title"] = "Kho tài nguyên & Đề thi";
}

<!-- Thêm Alpine.js cho bộ lọc và modal -->
<script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>

<div class="space-y-6" x-data="{ 
    ...shareManager(),
    isUploading: false, 
    uploadProgress: 0,
    isUploadingFile: false,
    activeSubject: 'All',
    activeFormat: 'All',
    searchQuery: '',
    viewingPdf: false, 
    currentPdfTitle: '', 
    currentPdfUrl: '',
    currentResourceId: null
}">
    
    <!-- Load Component Scripts -->
    <script defer src="/js/components/resource-discussion.js"></script>
    <script defer src="/js/components/premium-pdf-viewer.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/pdf.js/3.11.174/pdf.min.js"></script>
    
    <!-- Hero Section -->
    <div class="relative rounded-3xl p-8 md:p-12 overflow-hidden bg-gradient-to-br from-indigo-900 via-cyan-800 to-slate-900 text-white shadow-2xl">
        <div class="absolute -right-20 -top-20 opacity-20 pointer-events-none">
            <svg class="w-96 h-96" fill="currentColor" viewBox="0 0 24 24"><path d="M12 2L15 9L22 12L15 15L12 22L9 15L2 12L9 9L12 2Z"/></svg>
        </div>
        <div class="relative z-10 max-w-2xl">
            <span class="inline-block bg-orange-500 text-white text-xs font-bold px-3 py-1 rounded-full mb-4 shadow-lg shadow-orange-500/30 uppercase tracking-wider">Mạng lưới Tri thức</span>
            <h1 class="text-4xl md:text-5xl font-black mb-4 tracking-tight leading-tight">
                Khám phá Kho Tàng <br/>Tài Nguyên Chuyên Sâu
            </h1>
            <p class="text-lg opacity-80 mb-8 font-medium">
                Nơi hàng ngàn Developer chia sẻ Ebook, Source Code và Tài liệu học tập. Đánh giá, lưu trữ và chia sẻ để kiếm Viral XP!
            </p>
            <div class="flex gap-4">
                @if (User.Identity?.IsAuthenticated == true)
                {
                    <button @@click="isUploading = !isUploading" class="bg-white text-cyan-900 hover:bg-slate-50 font-bold px-6 py-3 rounded-xl transition-all shadow-md flex items-center gap-2">
                        <img src="/images/icons/social_share.svg" class="w-5 h-5 text-cyan-600" />
                        Tải lên Tài liệu
                    </button>
                }
                else
                {
                    <a href="/Auth/Login?returnUrl=/hub/resources" class="bg-white text-cyan-900 hover:bg-slate-50 font-bold px-6 py-3 rounded-xl transition-all shadow-md flex items-center gap-2">
                        <img src="/images/icons/course_locked.svg" class="w-5 h-5" />
                        Đăng nhập để Tải lên
                    </a>
                }
                <button class="border border-white/30 text-white hover:bg-white/10 font-bold px-6 py-3 rounded-xl transition-colors">
                    Xem Bảng xếp hạng
                </button>
            </div>
        </div>
    </div>

    <!-- Search and filter header -->
    <div class="flex flex-col md:flex-row items-center justify-between gap-4 bg-white p-4 rounded-2xl border border-slate-100 shadow-sm">
        <div class="flex-1 w-full relative">
            <input type="text" x-model="searchQuery" placeholder="Tìm kiếm sách, tài liệu, code..." class="w-full pl-10 pr-4 py-2.5 bg-slate-50 border-none rounded-xl text-sm font-medium text-slate-800 focus:ring-2 focus:ring-cyan-500 outline-none transition-all" />
            <svg class="w-5 h-5 text-slate-400 absolute left-3 top-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path></svg>
        </div>
        
        <div class="flex flex-wrap gap-2 items-center">
            <span class="text-xs font-semibold uppercase tracking-wider text-slate-400 mr-2 flex items-center gap-1">
                <img src="/images/icons/course_read_doc.svg" class="w-3.5 h-3.5 text-slate-400" /> Môn học:
            </span>
            <button @@click="activeSubject = 'All'" :class="activeSubject === 'All' ? 'bg-cyan-600 text-white shadow-sm' : 'bg-slate-50 text-slate-600 hover:bg-slate-100'" class="px-3 py-1.5 rounded-xl text-sm font-medium transition-all">
                Tất cả
            </button>
            <button @@click="activeSubject = '.NET Development'" :class="activeSubject === '.NET Development' ? 'bg-cyan-600 text-white shadow-sm' : 'bg-slate-50 text-slate-600 hover:bg-slate-100'" class="px-3 py-1.5 rounded-xl text-sm font-medium transition-all">
                .NET
            </button>
            <button @@click="activeSubject = 'Database Systems'" :class="activeSubject === 'Database Systems' ? 'bg-cyan-600 text-white shadow-sm' : 'bg-slate-50 text-slate-600 hover:bg-slate-100'" class="px-3 py-1.5 rounded-xl text-sm font-medium transition-all">
                Database
            </button>

            <!-- Pipe separator -->
            <div class="w-px h-6 bg-slate-200 mx-2 hidden lg:block"></div>

            <button @@click="activeFormat = 'All'" :class="activeFormat === 'All' ? 'text-indigo-600 bg-indigo-50 font-bold' : 'text-slate-400 hover:text-indigo-600'" class="text-xs px-2 py-1 rounded-md transition-all">Tất cả định dạng</button>
            <button @@click="activeFormat = 'PDF'" :class="activeFormat === 'PDF' ? 'text-indigo-600 bg-indigo-50 font-bold' : 'text-slate-400 hover:text-indigo-600'" class="text-xs px-2 py-1 rounded-md flex items-center gap-1 transition-all"><span class="w-1.5 h-1.5 rounded-full bg-rose-500"></span> PDFs</button>
            <button @@click="activeFormat = 'ZIP'" :class="activeFormat === 'ZIP' ? 'text-indigo-600 bg-indigo-50 font-bold' : 'text-slate-400 hover:text-indigo-600'" class="text-xs px-2 py-1 rounded-md flex items-center gap-1 transition-all"><span class="w-1.5 h-1.5 rounded-full bg-amber-500"></span> Source Code</button>
        </div>
    </div>

    <!-- Upload Slide Panel -->
    <div 
        x-show="isUploading"
        x-transition:enter="transition ease-out duration-200"
        x-transition:enter-start="opacity-0 -translate-y-2"
        x-transition:enter-end="opacity-100 translate-y-0"
        class="overflow-hidden" style="display: none;"
    >
        <form class="bg-white rounded-2xl p-6 border border-slate-100 shadow-sm space-y-4">
            <div class="flex justify-between items-center pb-2 border-b border-slate-100">
                <h3 class="font-bold text-slate-800 text-sm flex items-center gap-2">
                    <img src="/images/icons/social_share.svg" class="w-4 h-4 text-cyan-500" />
                    Chia sẻ tài liệu & Đề thi thử
                </h3>
                <button type="button" @@click="isUploading = false" class="text-xs text-slate-400 hover:text-slate-600 font-bold">
                    Hủy bỏ
                </button>
            </div>

            <div class="space-y-3">
                <input type="text" required placeholder="Tiêu đề tài liệu (ví dụ: Tổng hợp đề thi thử DSA kỳ 1 năm 2025)" class="w-full bg-slate-50 border-none rounded-xl px-4 py-3 text-slate-800 font-medium placeholder-slate-400 outline-none focus:ring-2 focus:ring-cyan-500 transition-all text-sm" />

                <textarea required placeholder="Mô tả nội dung tài liệu, cách tải hoặc tóm tắt các điểm quan trọng..." rows="3" class="w-full bg-slate-50 border-none rounded-xl p-4 text-slate-800 placeholder-slate-400 outline-none focus:ring-2 focus:ring-cyan-500 resize-none transition-all text-sm"></textarea>

                <div class="grid grid-cols-4 gap-3">
                    <div class="col-span-2 space-y-1">
                        <label class="text-xs font-semibold text-slate-400">Môn học / Lĩnh vực</label>
                        <select class="w-full bg-slate-50 border-none text-sm px-4 py-2.5 rounded-xl text-slate-700 outline-none focus:ring-2 focus:ring-cyan-500">
                            <option value=".NET Development">.NET Development</option>
                            <option value="Database Systems">Database Systems</option>
                            <option value="General Info">General Info</option>
                        </select>
                    </div>

                    <div class="space-y-1">
                        <label class="text-xs font-semibold text-slate-400">Định dạng</label>
                        <select class="w-full bg-slate-50 border-none text-sm px-4 py-2.5 rounded-xl text-slate-700 outline-none focus:ring-2 focus:ring-cyan-500">
                            <option value="PDF">PDF</option>
                            <option value="DOCX">DOCX</option>
                            <option value="ZIP">ZIP</option>
                        </select>
                    </div>

                    <div class="space-y-1">
                        <label class="text-xs font-semibold text-slate-400">File tài liệu</label>
                        <input type="file" id="uploadFile" accept=".pdf,.docx,.zip" class="w-full bg-slate-50 border-none text-sm px-4 py-2 rounded-xl text-slate-700 outline-none focus:ring-2 focus:ring-cyan-500" />
                    </div>
                </div>
            </div>

            <div class="flex justify-end pt-2 items-center gap-4">
                <!-- Progress bar area -->
                <div x-show="isUploadingFile" class="flex-1 max-w-xs flex items-center gap-2" style="display: none;">
                    <div class="w-full bg-slate-100 rounded-full h-2.5">
                        <div class="bg-cyan-600 h-2.5 rounded-full transition-all duration-300" :style="`width: ${uploadProgress}%`"></div>
                    </div>
                    <span class="text-xs font-bold text-slate-500" x-text="`${uploadProgress}%`"></span>
                </div>

                <button type="button" @@click="uploadResource()" :disabled="isUploadingFile" :class="isUploadingFile ? 'bg-slate-400 cursor-not-allowed' : 'bg-cyan-600 hover:bg-cyan-700 active:scale-95 shadow-md'" class="text-white font-bold text-sm px-6 py-2 rounded-xl transition-all">
                    <span x-show="!isUploadingFile">Xác nhận tải lên</span>
                    <span x-show="isUploadingFile" class="flex items-center gap-2">
                        <svg class="animate-spin h-4 w-4 text-white" fill="none" viewBox="0 0 24 24"><circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle><path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path></svg> Đang tải lên...
                    </span>
                </button>
            </div>
        </form>
    </div>

    <!-- Grid of Resources -->
    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
        @if (Model != null && Model.Any())
        {
            foreach (var res in Model)
            {
                var titleEscaped = Uri.EscapeDataString(res.Title ?? "");
                <div 
                    x-show="(activeSubject === 'All' || activeSubject === '@res.Subject') && (activeFormat === 'All' || activeFormat === '@res.FileType') && ('@res.Title.ToLower()'.includes(searchQuery.toLowerCase()) || '@res.Description?.ToLower()'.includes(searchQuery.toLowerCase()))"
                    x-transition:enter="transition ease-out duration-200"
                    x-transition:enter-start="opacity-0 scale-95"
                    x-transition:enter-end="opacity-100 scale-100"
                    class="bg-white rounded-2xl border border-slate-100 flex flex-col hover:shadow-xl hover:border-cyan-200 transition-all group overflow-hidden"
                    x-data="{ 
                        isBookmarked: @(res.IsBookmarkedByCurrentUser.ToString().ToLower()),
                        myRating: @res.CurrentUserRating,
                        downloads: @res.DownloadCount,
                        views: @res.ViewCount,
                        voteCount: @res.VoteCount,
                        avgRating: @res.Rating.ToString("0.0", System.Globalization.CultureInfo.InvariantCulture)
                    }"
                >
                    <!-- Header Cover (Thay vì Card ngang, làm Card dọc có banner) -->
                    <div class="h-28 bg-gradient-to-r from-slate-100 to-indigo-50 relative p-4 flex justify-between items-start">
                        <span class="bg-cyan-600 text-white text-[10px] font-bold px-3 py-1 rounded-full shadow-sm">@res.Subject</span>
                        <div class="flex gap-2">
                            <button @@click.stop="toggleBookmark(@res.Id)" class="p-2 bg-white rounded-full shadow-sm hover:scale-110 transition-transform">
                                <svg class="w-4 h-4" :class="isBookmarked ? 'fill-cyan-500 text-cyan-500' : 'text-slate-400'" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 5a2 2 0 012-2h10a2 2 0 012 2v16l-7-3.5L5 21V5z"></path></svg>
                            </button>
                            <button @@click.stop="openShare('RESOURCE', @res.Id)" class="p-2 bg-white rounded-full shadow-sm hover:scale-110 transition-transform group-hover:text-orange-500">
                                <img src="/images/icons/social_share.svg" class="w-4 h-4" />
                            </button>
                        </div>
                        
                        <!-- File Type Icon nổi ở góc trái -->
                        <div class="absolute -bottom-6 left-5 w-12 h-14 bg-white rounded-xl shadow-md flex flex-col items-center justify-center border border-slate-100 text-cyan-600 font-black">
                            <img src="/images/icons/course_read_doc.svg" class="w-5 h-5 mb-0.5" />
                            <span class="text-[9px] uppercase tracking-wider">@(res.FileType ?? "PDF")</span>
                        </div>
                    </div>

                    <!-- Content -->
                    <div class="p-5 pt-8 flex-1 flex flex-col">
                        <h4 @@click="viewingPdf = true; currentPdfTitle = decodeURIComponent('@titleEscaped'); currentPdfUrl = '@res.FileUrl' || 'https://www.w3.org/WAI/ER/tests/xhtml/testfiles/resources/pdf/dummy.pdf'; currentResourceId = @res.Id; recordView(@res.Id);"
                            class="font-extrabold text-slate-800 text-lg mb-2 leading-snug hover:text-cyan-600 transition-colors cursor-pointer line-clamp-2">
                            @res.Title
                        </h4>
                        
                        <p class="text-slate-500 text-xs line-clamp-2 leading-relaxed flex-1 mb-4">
                            @res.Description
                        </p>

                        <!-- Thống kê (Stars & Views) -->
                        <div class="bg-slate-50 rounded-xl p-3 mb-4 border border-slate-100 flex items-center justify-between">
                            <div class="flex items-center gap-1 group/rating relative">
                                <!-- Star Rating -->
                                <template x-for="i in 5">
                                    <svg @@click="submitRate(@res.Id, i)" class="w-4 h-4 cursor-pointer hover:scale-125 transition-transform" :class="(myRating >= i || (myRating === 0 && avgRating >= i)) ? 'text-amber-400 fill-amber-400' : 'text-slate-300'" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11.049 2.927c.3-.921 1.603-.921 1.902 0l1.519 4.674a1 1 0 00.95.69h4.915c.969 0 1.371 1.24.588 1.81l-3.976 2.888a1 1 0 00-.363 1.118l1.518 4.674c.3.922-.755 1.688-1.538 1.118l-3.976-2.888a1 1 0 00-1.176 0l-3.976 2.888c-.783.57-1.838-.197-1.538-1.118l1.518-4.674a1 1 0 00-.363-1.118l-3.976-2.888c-.784-.57-.38-1.81.588-1.81h4.914a1 1 0 00.951-.69l1.519-4.674z"></path></svg>
                                </template>
                                <span class="text-xs font-bold text-slate-500 ml-1">(<span x-text="voteCount"></span>)</span>
                            </div>
                            
                            <div class="flex items-center gap-3 text-slate-400">
                                <span class="flex items-center gap-1 text-xs font-bold"><svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"></path></svg> <span x-text="views"></span></span>
                                <span class="flex items-center gap-1 text-xs font-bold text-indigo-500"><svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4"></path></svg> <span x-text="downloads"></span></span>
                            </div>
                        </div>

                        <!-- Footer -->
                        <div class="flex items-center justify-between mt-auto">
                            <div class="flex items-center gap-2">
                                <div class="w-6 h-6 rounded-full overflow-hidden bg-slate-200">
                                    <img src="https://api.dicebear.com/7.x/avataaars/svg?seed=@res.UploaderId" alt="Author" />
                                </div>
                                <span class="font-bold text-xs text-slate-600 truncate max-w-[120px]">@(res.Uploader?.FullName ?? "Member")</span>
                            </div>
                            <button @@click.stop="viewingPdf = true; currentPdfTitle = decodeURIComponent('@titleEscaped'); currentPdfUrl = '@res.FileUrl' || 'https://www.w3.org/WAI/ER/tests/xhtml/testfiles/resources/pdf/dummy.pdf'; currentResourceId = @res.Id; recordView(@res.Id);" class="bg-cyan-50 hover:bg-cyan-100 text-cyan-700 px-4 py-1.5 rounded-lg text-xs font-bold transition-colors">
                                Mở xem
                            </button>
                        </div>
                    </div>
                </div>
            }
        }
        else
        {
            <div class="col-span-1 md:col-span-2 bg-white border border-slate-100 rounded-2xl p-12 text-center text-slate-400 space-y-2">
                <i data-lucide="file" class="mx-auto text-slate-300 w-8 h-8"></i>
                <p class="font-bold">Chưa có tài liệu nào.</p>
            </div>
        }
    </div>

    <!-- TÍCH HỢP PREMIUM PDF VIEWER MODAL -->
    <template x-teleport="body">
        <div x-show="viewingPdf" 
             class="fixed inset-0 z-[9999] flex items-center justify-center p-4 sm:p-6 font-sans" 
             style="display: none;">
            
            <!-- Backdrop mờ tối -->
            <div class="absolute inset-0 bg-slate-900/90 backdrop-blur-md" 
                 x-show="viewingPdf" 
                 x-transition.opacity 
                 @@click="viewingPdf = false"></div>
                 
            <!-- Modal Content - Thiết kế Glassmorphism -->
            <div class="relative w-full max-w-6xl h-[92vh] bg-slate-900/80 backdrop-blur-xl rounded-3xl shadow-[0_0_50px_rgba(8,145,178,0.15)] overflow-hidden flex flex-col border border-slate-700/50"
                 x-show="viewingPdf" 
                 x-transition:enter="transition ease-out duration-300"
                 x-transition:enter-start="opacity-0 scale-95 translate-y-8"
                 x-transition:enter-end="opacity-100 scale-100 translate-y-0"
                 x-transition:leave="transition ease-in duration-200"
                 x-transition:leave-start="opacity-100 scale-100 translate-y-0"
                 x-transition:leave-end="opacity-0 scale-95 translate-y-8">
                 
                <!-- Ánh sáng trang trí glow -->
                <div class="absolute -top-40 -right-40 w-96 h-96 bg-cyan-500/20 rounded-full blur-[100px] pointer-events-none"></div>
                <div class="absolute -bottom-40 -left-40 w-96 h-96 bg-indigo-500/20 rounded-full blur-[100px] pointer-events-none"></div>

                <!-- Header Document Viewer -->
                <div class="relative z-10 flex items-center justify-between px-6 py-4 bg-slate-900/50 border-b border-slate-700/50">
                    <div class="flex items-center gap-4">
                        <div class="w-10 h-10 rounded-xl bg-gradient-to-br from-rose-500 to-rose-600 text-white flex items-center justify-center shadow-lg shadow-rose-500/30">
                            <img src="/images/icons/course_read_doc.svg" class="w-5 h-5" />
                        </div>
                        <div>
                            <h3 class="text-base font-black text-white leading-tight truncate max-w-2xl tracking-wide" x-text="currentPdfTitle">Loading...</h3>
                            <p class="text-[11px] text-cyan-400 font-bold tracking-widest uppercase mt-0.5 flex items-center gap-1.5">
                                <span class="w-1.5 h-1.5 rounded-full bg-cyan-400 animate-pulse"></span>
                                SmartLMS Secure Viewer
                            </p>
                        </div>
                    </div>
                    <div class="flex items-center gap-3">
                        <a :href="currentPdfUrl" target="_blank" class="flex items-center gap-2 px-4 py-2 bg-slate-800 hover:bg-slate-700 text-slate-300 hover:text-white rounded-xl transition-all font-bold text-xs shadow-sm border border-slate-600/50">
                            <img src="/images/icons/course_download.svg" class="w-4 h-4" />
                            <span class="hidden sm:inline">Tải về máy</span>
                        </a>
                        <button @@click="viewingPdf = false" class="p-2 bg-rose-500/10 text-rose-400 hover:text-white hover:bg-rose-500 rounded-xl transition-all" title="Đóng">
                            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path></svg>
                        </button>
                    </div>
                </div>

                <!-- Viewer Body -->
                <div class="relative z-10 flex-1 p-4 sm:p-8 overflow-y-auto flex flex-col gap-8 custom-scrollbar">
                    <!-- Khung Canvas hiển thị PDF (Premium PDF Viewer) -->
                    <div class="w-full h-[70vh] min-h-[500px] bg-slate-900 rounded-2xl border border-slate-700/50 overflow-hidden shadow-inner flex flex-col"
                         x-data="premiumPdfViewer()" 
                         x-init="$watch('currentPdfUrl', value => { if(value) initViewer(value); })">
                        
                        <!-- Toolbar ảo của PDF.js -->
                        <div class="h-12 bg-slate-800 flex items-center justify-between px-4 border-b border-slate-700 flex-shrink-0 relative z-20 shadow-md">
                            <div class="flex items-center gap-4">
                                <div class="flex gap-1.5 hidden sm:flex">
                                    <div class="w-3 h-3 rounded-full bg-rose-500 shadow-[0_0_8px_rgba(244,63,94,0.6)]"></div>
                                    <div class="w-3 h-3 rounded-full bg-amber-500 shadow-[0_0_8px_rgba(245,158,11,0.6)]"></div>
                                    <div class="w-3 h-3 rounded-full bg-emerald-500 shadow-[0_0_8px_rgba(16,185,129,0.6)]"></div>
                                </div>
                                <div class="h-6 w-px bg-slate-700 hidden sm:block"></div>
                                <!-- Dark Mode Toggle -->
                                <button @@click="toggleDarkMode()" class="flex items-center gap-2 px-3 py-1.5 rounded-lg text-xs font-bold transition-all" :class="isDarkMode ? 'bg-cyan-900/40 text-cyan-400' : 'bg-slate-700 text-slate-300'">
                                    <svg x-show="!isDarkMode" class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z"></path></svg>
                                    <svg x-show="isDarkMode" class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z"></path></svg>
                                    <span class="hidden md:inline" x-text="isDarkMode ? 'Dark Mode' : 'Light Mode'"></span>
                                </button>
                            </div>

                            <!-- Pagination Controls -->
                            <div class="flex items-center gap-3 bg-slate-900 px-3 py-1.5 rounded-xl border border-slate-700">
                                <button @@click="prevPage()" :disabled="pageNum <= 1" class="text-slate-400 hover:text-white disabled:opacity-30 transition-colors p-1">
                                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path></svg>
                                </button>
                                <span class="text-xs font-bold text-slate-300 font-mono tracking-widest">
                                    Trang <span x-text="pageNum" class="text-white"></span> / <span x-text="totalPages"></span>
                                </span>
                                <button @@click="nextPage()" :disabled="pageNum >= totalPages" class="text-slate-400 hover:text-white disabled:opacity-30 transition-colors p-1">
                                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path></svg>
                                </button>
                            </div>

                            <!-- Zoom Controls -->
                            <div class="flex items-center gap-1">
                                <button @@click="zoomOut()" class="p-2 text-slate-400 hover:text-white hover:bg-slate-700 rounded-lg transition-colors">
                                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0zM13 10H7"></path></svg>
                                </button>
                                <span class="text-[10px] font-bold text-cyan-400 w-10 text-center" x-text="Math.round(scale * 100) + '%'"></span>
                                <button @@click="zoomIn()" class="p-2 text-slate-400 hover:text-white hover:bg-slate-700 rounded-lg transition-colors">
                                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0zM10 7v3m0 0v3m0-3h3m-3 0H7"></path></svg>
                                </button>
                            </div>
                        </div>

                        <!-- Nội dung Canvas Render PDF -->
                        <div class="flex-1 overflow-auto bg-[#1a1c23] flex justify-center p-4 relative custom-scrollbar">
                            <div x-show="isLoadingPdf" class="absolute inset-0 flex flex-col items-center justify-center bg-slate-900/80 z-10 backdrop-blur-sm">
                                <svg class="animate-spin h-10 w-10 text-cyan-500 mb-4" fill="none" viewBox="0 0 24 24"><circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle><path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path></svg>
                                <p class="text-cyan-400 font-bold tracking-widest uppercase text-sm animate-pulse">Đang giải mã PDF...</p>
                            </div>
                            <!-- Áp dụng CSS Filter Invert khi bật Dark Mode -->
                            <canvas x-ref="pdfCanvas" class="shadow-2xl transition-all duration-300" 
                                    :style="isDarkMode ? 'filter: invert(90%) hue-rotate(180deg) contrast(1.1);' : 'filter: none;'">
                            </canvas>
                        </div>
                    </div>

                    <!-- Bottom Panel Tương tác (Thảo luận đa tầng) -->
                    <div class="w-full flex flex-col gap-4" x-data="resourceDiscussion()" x-init="$watch('currentResourceId', value => { if(value) openResource(value); })">
                        <div class="bg-slate-800/50 border border-slate-700/50 rounded-2xl p-4 backdrop-blur-sm flex justify-between items-center">
                            <h4 class="text-sm font-black text-slate-300 uppercase tracking-widest flex items-center gap-2">
                                <svg class="w-5 h-5 text-cyan-500" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8h2a2 2 0 012 2v6a2 2 0 01-2 2h-2v4l-4-4H9a1.994 1.994 0 01-1.414-.586m0 0L11 14h4a2 2 0 002-2V6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2v4l.586-.586z"></path></svg>
                                Thảo luận cộng đồng (<span x-text="comments.length"></span>)
                            </h4>
                            <button @@click="openReport(null)" class="text-xs text-rose-400 hover:text-rose-300 font-bold flex items-center gap-1"><img src="/images/icons/course_locked.svg" class="w-3 h-3"/> Báo cáo tài liệu</button>
                        </div>

                        <!-- Form Bình luận Gốc -->
                        <div class="bg-slate-800/50 border border-slate-700/50 rounded-2xl p-5 backdrop-blur-sm shadow-lg">
                            <div class="flex gap-4">
                                <div class="w-10 h-10 rounded-full bg-slate-700 flex-shrink-0 flex items-center justify-center">
                                    <svg class="w-5 h-5 text-slate-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path></svg>
                                </div>
                                <div class="flex-1">
                                    <textarea x-model="newComment" rows="3" class="w-full bg-slate-900 border border-slate-700 rounded-xl p-4 text-sm text-slate-300 focus:outline-none focus:border-cyan-500 focus:ring-1 focus:ring-cyan-500 resize-none transition-all" placeholder="Bạn có thắc mắc gì về tài liệu này? Hãy gõ @AI nếu bạn muốn trợ lý ảo giải đáp tự động..."></textarea>
                                    <div class="flex justify-between items-center mt-3">
                                        <div class="text-xs text-slate-500 italic">Mẹo: Bạn có thể sử dụng Markdown và gõ <strong class="text-cyan-400">@AI</strong> để gọi bot.</div>
                                        <button @@click="postComment(null)" :disabled="!newComment.trim() || isLoading" class="bg-cyan-600 hover:bg-cyan-500 disabled:bg-slate-700 disabled:text-slate-500 text-white font-bold text-sm px-6 py-2.5 rounded-xl transition-all shadow-md flex items-center gap-2">
                                            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 19l9 2-9-18-9 18 9-2zm0 0v-8"></path></svg>
                                            Gửi bình luận
                                        </button>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- Danh sách bình luận -->
                        <div class="bg-slate-800/50 border border-slate-700/50 rounded-2xl p-5 backdrop-blur-sm flex flex-col">
                            <div x-show="isLoading" class="text-slate-400 text-sm text-center py-8 flex flex-col items-center gap-3">
                                <svg class="animate-spin h-6 w-6 text-cyan-500" fill="none" viewBox="0 0 24 24"><circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle><path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path></svg>
                                Đang tải bình luận...
                            </div>
                            <div x-show="!isLoading && comments.length === 0" class="text-slate-500 text-sm text-center py-10 border-2 border-dashed border-slate-700 rounded-xl">
                                Chưa có thảo luận nào. Hãy là người đầu tiên đặt câu hỏi!
                            </div>
                            
                            <div class="space-y-6 mt-2">
                                <template x-for="comment in comments" :key="comment.id">
                                    <div class="text-sm border-b border-slate-700/50 pb-5 last:border-0">
                                        <div class="flex items-start justify-between">
                                            <div class="flex items-center gap-3 mb-2">
                                                <img :src="comment.authorAvatar" class="w-10 h-10 rounded-full bg-slate-700 border-2 border-slate-600 shadow-sm" />
                                                <div>
                                                    <div class="flex items-center gap-2">
                                                        <span class="font-bold text-slate-200 text-sm" x-text="comment.authorName"></span>
                                                        <span x-show="comment.authorName === '🤖 SmartLMS AI'" class="bg-indigo-500/20 text-indigo-400 text-[9px] px-1.5 py-0.5 rounded uppercase font-black tracking-wider border border-indigo-500/30">AI Bot</span>
                                                    </div>
                                                    <span class="text-xs text-slate-500" x-text="new Date(comment.createdAt).toLocaleString()"></span>
                                                </div>
                                            </div>
                                            <button @@click="openReport(comment.id)" class="text-slate-600 hover:text-rose-400 p-1 transition-colors" title="Báo cáo vi phạm">
                                                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 21v-4m0 0V5a2 2 0 012-2h6.5l1 1H21l-3 6 3 6h-8.5l-1-1H5a2 2 0 00-2 2zm9-13.5V9"></path></svg>
                                            </button>
                                        </div>
                                        
                                        <p class="text-slate-300 text-sm leading-relaxed ml-13 mb-3 whitespace-pre-wrap" x-text="comment.content"></p>
                                        
                                        <div class="ml-13 flex items-center gap-4 text-xs font-bold text-slate-400">
                                            <button @@click="upvote(comment.id)" class="hover:text-cyan-400 flex items-center gap-1 bg-slate-800/80 px-2.5 py-1 rounded-md transition-colors border border-slate-700"><span class="text-[10px]">▲</span> <span x-text="comment.upvotes"></span></button>
                                            <button @@click="activeReplyId = activeReplyId === comment.id ? null : comment.id" class="hover:text-cyan-400 flex items-center gap-1 transition-colors">
                                                <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 10h10a8 8 0 018 8v2M3 10l6 6m-6-6l6-6"></path></svg> Phản hồi
                                            </button>
                                            <button @@click="deleteComment(comment.id)" class="hover:text-rose-400 transition-colors ml-auto">Xóa</button>
                                        </div>

                                        <!-- Form Trả lời -->
                                        <div x-show="activeReplyId === comment.id" class="ml-13 mt-4 flex gap-3 items-start" x-transition>
                                            <img src="https://api.dicebear.com/7.x/avataaars/svg?seed=CurrentUser" class="w-8 h-8 rounded-full bg-slate-700 opacity-50" />
                                            <div class="flex-1">
                                                <textarea x-model="replyContent" @@keydown.enter="postComment(comment.id)" rows="2" class="w-full bg-slate-900 border border-slate-700 rounded-xl p-3 text-sm text-slate-300 focus:outline-none focus:border-cyan-500 focus:ring-1 focus:ring-cyan-500 resize-none transition-all" placeholder="Viết phản hồi..."></textarea>
                                                <div class="flex justify-end gap-2 mt-2">
                                                    <button @@click="activeReplyId = null" class="px-4 py-1.5 text-xs text-slate-400 hover:text-slate-300 font-bold">Hủy</button>
                                                    <button @@click="postComment(comment.id)" class="bg-cyan-600 hover:bg-cyan-500 text-white px-5 py-1.5 rounded-lg text-xs font-bold transition-colors">Gửi</button>
                                                </div>
                                            </div>
                                        </div>

                                        <!-- Replies -->
                                        <div class="ml-13 mt-4 space-y-4 border-l-2 border-slate-700 pl-4">
                                            <template x-for="reply in comment.replies" :key="reply.id">
                                                <div class="bg-slate-900/50 p-3.5 rounded-xl border border-slate-800">
                                                    <div class="flex items-start justify-between">
                                                        <div class="flex items-center gap-2 mb-1.5">
                                                            <img :src="reply.authorAvatar" class="w-7 h-7 rounded-full bg-slate-700 border border-slate-600" />
                                                            <div>
                                                                <div class="flex items-center gap-1.5">
                                                                    <span class="font-bold text-slate-300 text-xs" x-text="reply.authorName"></span>
                                                                    <span x-show="reply.authorName === '🤖 SmartLMS AI'" class="bg-indigo-500/20 text-indigo-400 text-[8px] px-1 py-0.5 rounded uppercase font-black tracking-wider border border-indigo-500/30">AI Bot</span>
                                                                </div>
                                                                <span class="text-[10px] text-slate-500" x-text="new Date(reply.createdAt).toLocaleString()"></span>
                                                            </div>
                                                        </div>
                                                        <button @@click="openReport(reply.id)" class="text-slate-600 hover:text-rose-400 transition-colors" title="Báo cáo">
                                                            <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 21v-4m0 0V5a2 2 0 012-2h6.5l1 1H21l-3 6 3 6h-8.5l-1-1H5a2 2 0 00-2 2zm9-13.5V9"></path></svg>
                                                        </button>
                                                    </div>
                                                    
                                                    <p class="text-slate-400 text-sm leading-relaxed ml-9 mb-2 whitespace-pre-wrap" x-text="reply.content"></p>
                                                    
                                                    <div class="ml-9 flex items-center gap-3 text-[11px] font-bold text-slate-500">
                                                        <button @@click="upvote(reply.id)" class="hover:text-cyan-400 flex items-center gap-1 bg-slate-800 px-2 py-0.5 rounded transition-colors"><span class="text-[9px]">▲</span> <span x-text="reply.upvotes"></span></button>
                                                        <button @@click="deleteComment(reply.id)" class="hover:text-rose-400 ml-auto transition-colors">Xóa</button>
                                                    </div>
                                                </div>
                                            </template>
                                        </div>
                                    </div>
                                </template>
                            </div>
                        </div>

                        <!-- Report Modal (Teleport to body to avoid z-index/overflow issues) -->
                        <template x-teleport="body">
                            <div x-show="showReportModal" class="fixed inset-0 z-[10000] flex items-center justify-center bg-black/50 backdrop-blur-sm" style="display: none;">
                                <div @@click.away="showReportModal = false" class="bg-white rounded-3xl w-full max-w-sm p-6 space-y-4 shadow-2xl">
                                    <h3 class="text-lg font-black text-slate-800 flex items-center gap-2">
                                        <img src="/images/icons/course_locked.svg" class="w-5 h-5 text-rose-500" /> Báo cáo vi phạm
                                    </h3>
                                    <p class="text-xs text-slate-500">Giúp chúng tôi giữ môi trường học tập trong sạch. Chọn lý do báo cáo:</p>
                                    <select x-model="reportReason" class="w-full p-3 bg-slate-50 border border-slate-200 rounded-xl outline-none focus:ring-2 focus:ring-rose-500 text-sm font-bold text-slate-700">
                                        <option value="">-- Chọn lý do --</option>
                                        <option value="Spam / Quảng cáo">Spam / Quảng cáo</option>
                                        <option value="Nội dung đồi trụy / Phản cảm">Nội dung đồi trụy / Phản cảm</option>
                                        <option value="Vi phạm bản quyền">Vi phạm bản quyền</option>
                                        <option value="Ngôn từ đả kích / Xúc phạm">Ngôn từ đả kích / Xúc phạm</option>
                                    </select>
                                    <div class="flex justify-end gap-3 pt-2">
                                        <button @@click="showReportModal = false" class="px-4 py-2 text-xs font-bold text-slate-500 hover:bg-slate-100 rounded-xl transition-colors">Hủy</button>
                                        <button @@click="submitReport" :disabled="!reportReason" class="px-4 py-2 bg-rose-600 text-white rounded-xl hover:bg-rose-700 disabled:bg-slate-400 transition-colors font-bold text-xs shadow-md">Gửi báo cáo</button>
                                    </div>
                                </div>
                            </div>
                        </template>
                    </div>
                </div>
            </div>
        </div>
    </template>

    <!-- Share Modal -->
    <div x-show="isOpen" class="fixed inset-0 z-[10000] flex items-center justify-center bg-black/50 backdrop-blur-sm" style="display: none;" x-transition>
        <div @@click.away="isOpen = false" class="bg-white rounded-3xl w-full max-w-md p-8 space-y-5 shadow-2xl">
            <h3 class="text-xl font-black text-slate-800 flex items-center gap-2">
                <img src="/images/icons/social_share.svg" class="w-5 h-5 text-indigo-600" /> Chuyển tiếp tài liệu
            </h3>
            
            <div class="space-y-3">
                <label class="text-xs font-bold text-slate-500 uppercase tracking-wider">Gửi đến</label>
                <select x-model="targetType" class="w-full p-3 bg-slate-50 border border-slate-200 rounded-xl outline-none focus:ring-2 focus:ring-indigo-500 text-sm font-bold text-slate-700">
                    <option value="Group">Một nhóm học (Group)</option>
                    <option value="User">Tin nhắn cá nhân (User)</option>
                </select>
            </div>
            
            <div class="space-y-3">
                <label class="text-xs font-bold text-slate-500 uppercase tracking-wider">ID Người nhận / Nhóm</label>
                <input type="number" x-model="targetId" class="w-full p-3 bg-slate-50 border border-slate-200 rounded-xl outline-none focus:ring-2 focus:ring-indigo-500 text-sm font-bold text-slate-700" placeholder="Nhập ID...">
            </div>
            
            <div class="space-y-3">
                <label class="text-xs font-bold text-slate-500 uppercase tracking-wider">Lời nhắn (Tùy chọn)</label>
                <textarea x-model="message" rows="2" class="w-full p-3 bg-slate-50 border border-slate-200 rounded-xl outline-none focus:ring-2 focus:ring-indigo-500 text-sm placeholder-slate-400 resize-none" placeholder="Hãy xem tài liệu này..."></textarea>
            </div>
            
            <div class="flex justify-end gap-3 pt-4 border-t border-slate-100">
                <button @@click="isOpen = false" class="px-5 py-2.5 text-sm font-bold text-slate-500 hover:bg-slate-100 rounded-xl transition-colors">Hủy</button>
                <button @@click="submitShare" :disabled="isSharing" class="px-5 py-2.5 bg-indigo-600 text-white rounded-xl hover:bg-indigo-700 disabled:bg-slate-400 transition-colors font-bold shadow-md flex items-center gap-2">
                    <span x-text="isSharing ? 'Đang gửi...' : 'Gửi đi'"></span>
                </button>
            </div>
        </div>
    </div>
</div>

<script>
    async function uploadResource() {
        const title = document.querySelector('input[placeholder*="Tiêu đề"]')?.value?.trim();
        const desc  = document.querySelector('textarea')?.value?.trim();
        const subject = document.querySelector('select')?.value || '.NET Development';
        const fileInput = document.getElementById('uploadFile');
        const file = fileInput?.files[0];
        
        if (!title || !file) {
            window.showToast && window.showToast('⚠️ Vui lòng nhập tiêu đề và chọn file!');
            return;
        }

        const formData = new FormData();
        formData.append('Title', title);
        formData.append('Description', desc);
        formData.append('Subject', subject);
        formData.append('FileType', file.name.split('.').pop().toUpperCase());
        formData.append('File', file);

        // Sử dụng Alpine data context
        const alpineData = Alpine.$data(document.querySelector('[x-data]'));
        alpineData.isUploadingFile = true;
        alpineData.uploadProgress = 0;

        try {
            const xhr = new XMLHttpRequest();
            xhr.open('POST', '/api/ResourceApi', true);
            
            xhr.upload.onprogress = function(e) {
                if (e.lengthComputable) {
                    alpineData.uploadProgress = Math.round((e.loaded / e.total) * 100);
                }
            };
            
            xhr.onload = function() {
                alpineData.isUploadingFile = false;
                if (xhr.status >= 200 && xhr.status < 300) {
                    const data = JSON.parse(xhr.responseText);
                    window.showToast && window.showToast('✅ ' + data.message);
                    setTimeout(() => location.reload(), 1500);
                } else if (xhr.status === 401) {
                    window.location.href = '/Auth/Login?returnUrl=/hub/resources';
                } else {
                    let err = "Lỗi tải lên.";
                    try { err = JSON.parse(xhr.responseText).message || err; } catch(e){}
                    window.showToast && window.showToast('❌ ' + err);
                }
            };
            
            xhr.onerror = function() {
                alpineData.isUploadingFile = false;
                window.showToast && window.showToast('❌ Lỗi kết nối.');
            };
            
            xhr.send(formData);
        } catch {
            alpineData.isUploadingFile = false;
            window.showToast && window.showToast('❌ Lỗi không xác định.');
        }
    }

    function shareManager() {
        return {
            isOpen: false, isSharing: false, contentType: '', contentId: 0,
            targetType: 'Group', targetId: '', message: '',

            openShare(type, id) {
                this.contentType = type; this.contentId = id;
                this.isOpen = true; this.targetId = ''; this.message = '';
            },

            async submitShare() {
                if (!this.targetId) return;
                this.isSharing = true;
                try {
                    const res = await fetch('/api/ShareApi/share', {
                        method: 'POST',
                        headers: { 'Content-Type': 'application/json' },
                        body: JSON.stringify({
                            targetType: this.targetType, targetId: parseInt(this.targetId),
                            contentType: this.contentType, contentId: this.contentId, message: this.message
                        })
                    });
                    if (res.ok) {
                        this.isOpen = false;
                        window.showToast && window.showToast('✅ Đã chia sẻ thành công!');
                    } else if (res.status === 401) window.location.href = '/Auth/Login';
                    else window.showToast && window.showToast('❌ Lỗi chia sẻ.');
                } finally { this.isSharing = false; }
            },

            // New API methods
            async toggleBookmark(id) {
                try {
                    const res = await fetch(`/api/ResourceApi/${id}/bookmark`, { method: 'POST' });
                    if (res.ok) {
                        this.isBookmarked = !this.isBookmarked;
                        window.showToast && window.showToast('✅ Đã cập nhật bộ sưu tập');
                    } else if (res.status === 401) window.location.href = '/Auth/Login';
                } catch(e) {}
            },

            async submitRate(id, score) {
                try {
                    const res = await fetch(`/api/ResourceApi/${id}/rate`, {
                        method: 'POST',
                        headers: { 'Content-Type': 'application/json' },
                        body: JSON.stringify({ score: score })
                    });
                    if (res.ok) {
                        this.myRating = score;
                        this.voteCount++;
                        window.showToast && window.showToast(`✅ Đã đánh giá ${score} sao!`);
                    } else if (res.status === 401) window.location.href = '/Auth/Login';
                } catch(e) {}
            },

            async recordView(id) {
                try {
                    await fetch(`/api/ResourceApi/${id}/view`, { method: 'POST' });
                    this.views++;
                } catch(e) {}
            }
        };
    }
</script>
